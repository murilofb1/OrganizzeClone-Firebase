<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.murilofb.organizzeclone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".slider.SliderAct"
            android:theme="@style/Theme.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.NovaDespesaActivity"
            android:theme="@style/DespesasActivityTheme" />
        <activity
            android:name=".activities.NovaReceitaActivity"
            android:theme="@style/ReceitasActivityTheme" />
        <activity
            android:name=".activities.MainScreenActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".authentication.LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".authentication.CadastroActivity"
            android:screenOrientation="portrait">

        </activity>
        <activity
            android:name=".slider.SliderActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Intro">

        </activity>
    </application>

</manifest>